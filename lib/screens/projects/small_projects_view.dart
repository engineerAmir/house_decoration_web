import 'package:flutter/material.dart';

class SmallProjectsView extends StatelessWidget {
  const SmallProjectsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
        
      ],
    );
  }
}
